services:
  postgresdb:
    image: postgres:17
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: abdul-coder
      POSTGRES_PASSWORD: abdul-coder
      POSTGRES_DB: ar_designshub
    volumes:
      - 'postgres_data:/var/lib/postgresql/data'

volumes:
  postgres_data:
